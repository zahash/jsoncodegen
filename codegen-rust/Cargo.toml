[package]
name = "jsoncodegen-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
convert_case = "0.10"
serde_json = "1"
syn = { version = "2", features = ["parsing"] }

jsoncodegen = { path = "../core" }
